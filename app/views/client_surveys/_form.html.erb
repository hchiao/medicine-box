<%= form_for(@client_survey) do |f| %>
  <% if @client_survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client_survey.errors.count, "error") %> prohibited this client_survey from being saved:</h2>

      <ul>
      <% @client_survey.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :survey_id, :value => @target_survey.id %>
  <%= f.fields_for :answers do |builder| %>
    <%= render "answer_fields", :f => builder %>
  <% end %>

  <div class="actions">
    <%= f.submit "Check it" %>
  </div>
<% end %>
