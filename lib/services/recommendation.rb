class Recommendation
    def make(parameter)
        puts "=========================================================================="
        puts parameter
        hash = {}
        hash
    end
end
